// Enable define to output debug information via the serial interface
//#define DEBUG

/// Pin number connected to the light sensor (one round: 6 high/low cycles)
const int PIN_LIGHT_SENSOR = 2;
/// Pin number connected to the hall sensor (one round: 1 high/low cycle)
const int PIN_HALL_SENSOR = 3;
/// Pin number connected to the ball release servo motor
const int PIN_SERVO = 9;
/// Pin number connected to the trigger switch (active high)
const int PIN_TRIGGER = 4;
/// Pin number connected to the first LED
const int PIN_LED1 = 12;
/// Pin number connected to the second LED
const int PIN_LED2 = 13;
